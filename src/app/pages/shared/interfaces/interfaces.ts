    export interface Mmebro{
      id: number;
      nome: string;
      contato: string;
    }

    export interface Doacao {
      id: number;
      valor: number;
      data: string;
    }

    // Removendo duplicação de tipos.
